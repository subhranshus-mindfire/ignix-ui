# 📄 `AnimatedTextarea` Component Documentation

## Overview

`AnimatedTextarea` is a highly customizable, animation-driven textarea component built with **React** and **Framer Motion**. It supports various animation variants that affect both label and textarea appearance, enhancing UX through visual cues like glowing borders, character counts, line highlights, and more.

---

## 🔧 Props

| Prop               | Type                               | Required | Description                                                                 |
|--------------------|------------------------------------|----------|-----------------------------------------------------------------------------|
| `placeholder`      | `string`                           | ✅       | Text displayed as the label before input begins.                            |
| `variant`          | `string`                           | ✅       | The animation style (must match a key from `textareaVariants`).             |
| `value`            | `string`                           | ✅       | The textarea's current value (controlled component).                        |
| `onChange`         | `(value: string) => void`          | ✅       | Callback function to update the `value`.                                    |
| `textareaClassName`| `string`                           | ❌       | Additional classes for the `<textarea>` element.                            |
| `labelClassName`   | `string`                           | ❌       | Additional classes for the `<label>` element.                               |

---

## 💡 Features by Variant

Some variants include extra features:

| Variant            | Description                                                                 |
|--------------------|-----------------------------------------------------------------------------|
| `expandable`       | Auto-resizes based on content height.                                       |
| `smoothExpand`     | Same as `expandable` but with animation smoothing.                          |
| `characterCount`   | Displays live character count (`value.length / 500`).                       |
| `lineHighlight`    | Highlights a line based on character count.                                 |
| `particleField`    | Shows a looping animation of floating particles around the textarea.        |
| `cosmic`, `wave`, `hologram`, etc. | Continuous animated effects for futuristic feel.            |

---

## 📦 Built-in Variants

The following are supported via the `variant` prop:

- `clean`
- `expandable`
- `smoothExpand`
- `glowBorder`
- `characterCount`
- `lineHighlight`
- `typewriterSound`
- `markdownPreview`
- `autoComplete`
- `syntaxHighlight`
- `rippleEffect`
- `gradientBorder`
- `neonGlow`
- `particleField`
- `elastic`
- `wave`
- `spotlight`
- `liquid`
- `cosmic`
- `hologram`

---

## 🧠 Internal Behavior

- Uses `Framer Motion`'s `motion.label` and `motion.textarea` for animated label and field transitions.
- Tracks `focus`, `blur`, and `value` changes for triggering animation states.
- `textareaRef` is used for calculating dynamic height (`scrollHeight`) for expanding variants.
- Extra UI elements like `div`s or labels are conditionally rendered based on the active variant.

---

## ✨ Example Usage

```tsx
<AnimatedTextarea
  placeholder="Write your message..."
  variant="characterCount"
  value={text}
  onChange={setText}
/>
```

---

## 📁 File Structure Note

All animation variants are declared in the `textareaVariants` object at the bottom of the component file, using the `TextareaVariant` type structure (`label`, `textarea`, optional `extra`).