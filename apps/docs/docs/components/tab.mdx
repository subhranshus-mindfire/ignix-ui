---
sidebar_position: 16
title: Tab
description: A flexible and animated tab component for displaying content and capturing user input.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { Tabs as TabsComponent } from '@site/src/components/UI/tab';

## Overview

The Tabs component is a versatile navigation interface that allows users to switch between different views within the same context. Built with Framer Motion for smooth animations and powered by Class Variance Authority for flexible styling, it provides multiple variants and themes for different design needs.

## Preview

<Tabs defaultValue="preview">
  <TabItem value="preview" label="Preview" default>
    <div className="w-full p-8 space-y-8 rounded-lg border shadow-lg">
      <div className="p-6 rounded-lg">
        <TabsComponent
          options={["Home", "Profile", "Settings", "About"]}
          selected={0}
        />
      </div>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">
    ```tsx
    import { Tabs } from '@mindfiredigital/ignix-ui';

    function TabsDemo() {
      return (
        <Tabs
          options={["Home", "Profile", "Settings", "About"]}
          selected={0}
        />
      );
    }
    ```
  </TabItem>
</Tabs>

## Installation

<Tabs>
  <TabItem value="npm" label="npm" default>
    ```bash
    npx @mindfiredigital/ignix-ui add tab
    ```
  </TabItem>
  <TabItem value="yarn" label="Yarn">
    ```bash
    yarn @mindfiredigital/ignix-ui add tab
    ```
  </TabItem>
  <TabItem value="pnpm" label="pnpm">
    ```bash
    pnpm @mindfiredigital/ignix-ui add tab
    ```
  </TabItem>
</Tabs>

## Usage

Import the component:

```tsx
import { Tabs } from '@mindfiredigital/ignix-ui';
```

### Basic Usage

```tsx
function BasicTabs() {
  return (
    <Tabs
      options={["Tab 1", "Tab 2", "Tab 3"]}
      selected={0}
      value={(index) => console.log(`Selected tab: ${index}`)}
    />
  );
}
```

## Variants

The Tabs component comes with several variants to suit different design needs.

### Style Variants

<Tabs>
  <TabItem value="style-preview" label="Preview" default>
    <div className="w-full p-8 space-y-8 border rounded-lg shadow-lg">
      <div className="p-6 rounded-lg shadow space-y-8">
        <div className="space-y-4">
          <span className="text-sm font-medium">Underline</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="underline"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Filled</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="filled"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Pill</span> 
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="pill"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Outline</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="outline"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Ghost</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="ghost"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Shadow</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="shadow"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Gradient</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="gradient"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Glow</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="glow"
          />
        </div>
      </div>
    </div>
  </TabItem>
  <TabItem value="style-code" label="Code">
    ```tsx
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="underline"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="filled"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="pill"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="outline"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="ghost"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="shadow"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="gradient"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="glow"
    />
    ```
  </TabItem>
</Tabs>

### Themes

<Tabs>
  <TabItem value="theme-preview" label="Preview" default>
    <div className="w-full p-8 space-y-8 rounded-lg border shadow-lg">
      <div className="p-6 rounded-lg shadow space-y-8">
        <div className="space-y-4">
          <span className="text-sm font-medium">Light Theme</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="filled"
            theme="light"
          />
        </div>
        <div className="space-y-4 bg-gray-900 p-4 rounded-lg">
          <span className="text-sm font-medium text-white">Dark Theme</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="filled"
            theme="dark"
          />
        </div>
        <div className="space-y-4 bg-gradient-to-r from-blue-500 to-purple-500 p-4 rounded-lg">
          <span className="text-sm font-medium text-white">Glass Theme</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="filled"
            theme="glass"
          />
        </div>
        <div className="space-y-4 bg-gradient-to-r from-blue-500 to-purple-500 p-4 rounded-lg">
          <span className="text-sm font-medium text-white">Glass Gradient Theme</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            variant="filled"
            theme="glassGradient"
          />
        </div>
      </div>
    </div>
  </TabItem>
  <TabItem value="theme-code" label="Code">
    ```tsx
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="filled"
      theme="light"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="filled"
      theme="dark"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="filled"
      theme="glass"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      variant="filled"
      theme="glassGradient"
    />
    ```
  </TabItem>
</Tabs>

### Size Variants

<Tabs>
  <TabItem value="size-preview" label="Preview" default>
    <div className="w-full p-8 space-y-8 rounded-lg border shadow-lg">
      <div className="p-6 rounded-lg shadow space-y-8">
        <div className="space-y-4">
          <span className="text-sm font-medium">Small</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            size="sm"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Medium</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            size="md"
          />
        </div>
        <div className="space-y-4">
          <span className="text-sm font-medium">Large</span>
          <TabsComponent
            options={["Home", "Profile", "Settings"]}
            size="lg"
          />
        </div>
      </div>
    </div>
  </TabItem>
  <TabItem value="size-code" label="Code">
    ```tsx
    <Tabs
      options={["Home", "Profile", "Settings"]}
      size="sm"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      size="md"
    />
    <Tabs
      options={["Home", "Profile", "Settings"]}
      size="lg"
    />
    ```
  </TabItem>
</Tabs>

## Interactive Example

<Tabs>
  <TabItem value="interactive-preview" label="Preview" default>
    <div className="w-full p-8 space-y-8 bg-slate-100 rounded-lg shadow-lg">
      <div className="bg-white p-6 rounded-lg shadow space-y-4">
        <TabsComponent
          options={["Account", "Security", "Notifications", "Integration"]}
          variant="outline"
          size="md"
          value={(index) => console.log(`Selected: ${index}`)}
        />
        <div className="mt-4 p-4 border rounded-lg">
          <p className="text-sm text-gray-600">Content area for the selected tab</p>
        </div>
      </div>
    </div>
  </TabItem>
  <TabItem value="interactive-code" label="Code">
    ```tsx
    function TabsWithContent() {
      const [activeTab, setActiveTab] = useState(0);
      
      const content = [
        "Account settings and preferences",
        "Security options and privacy",
        "Notification preferences",
        "Third-party integrations",
      ];

      return (
        <div className="space-y-4">
          <Tabs
            options={[
              "Account",
              "Security",
              "Notifications",
              "Integration"
            ]}
            variant="outline"
            size="md"
            value={setActiveTab}
          />
          <div className="mt-4 p-4 border rounded-lg">
            <p className="text-sm text-gray-600">
              {content[activeTab]}
            </p>
          </div>
        </div>
      );
    }
    ```
  </TabItem>
</Tabs>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `string[]` | `[]` | Array of tab labels |
| `selected` | `number` | `0` | Index of the initially selected tab |
| `value` | `(index: number) => void` | - | Callback function when tab selection changes |
| `variant` | `'underline' \| 'filled' \| 'pill' \| 'outline' \| 'ghost' \| 'shadow' \| 'gradient' \| 'glow' \| 'block'` | `'underline'` | Visual style variant |
| `theme` | `'light' \| 'dark' \| 'glass' \| 'glassDark' \| 'glassLight' \| 'glassGradient' \| 'glassGradientDark'` | - | Theme variant |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Size variant |
| `className` | `string` | - | Additional CSS classes |

## Customization

### Custom Styling

You can customize the appearance using Tailwind CSS classes:

```tsx
<Tabs
  options={["Tab 1", "Tab 2", "Tab 3"]}
  className="bg-indigo-100 p-2 rounded-xl"
/>
```

### Advanced Example with Content

```tsx
function TabsWithPanels() {
  const [activeTab, setActiveTab] = useState(0);

  const tabPanels = [
    {
      title: "Dashboard",
      content: (
        <div className="p-4">
          <h3 className="text-lg font-medium">Dashboard Overview</h3>
          <p className="mt-2 text-gray-600">Your dashboard content here</p>
        </div>
      ),
    },
    {
      title: "Analytics",
      content: (
        <div className="p-4">
          <h3 className="text-lg font-medium">Analytics Data</h3>
          <p className="mt-2 text-gray-600">Your analytics content here</p>
        </div>
      ),
    },
    {
      title: "Settings",
      content: (
        <div className="p-4">
          <h3 className="text-lg font-medium">Settings Panel</h3>
          <p className="mt-2 text-gray-600">Your settings content here</p>
        </div>
      ),
    },
  ];

  return (
    <div className="space-y-4">
      <Tabs
        options={tabPanels.map(tab => tab.title)}
        variant="gradient"
        theme="glass"
        value={setActiveTab}
      />
      <div className="mt-4 border rounded-lg overflow-hidden">
        {tabPanels[activeTab].content}
      </div>
    </div>
  );
}
```