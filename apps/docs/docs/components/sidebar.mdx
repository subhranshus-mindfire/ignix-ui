---
sidebar_position: 10
title: Sidebar
description: A flexible and animated sidebar component for displaying content.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Sidebar from '@site/src/components/UI/sidebar';
import { Home, Settings, User, Mail, HelpCircle } from 'lucide-react';

## Overview

The Sidebar component is a versatile and animated navigation element that provides an elegant way to organize navigation links and actions in your application. It supports multiple positions, variants, and animations built with Framer Motion.

## Preview

<Tabs defaultValue="preview">
  <TabItem value="preview" label="Preview" default>
    <div className="w-full h-[400px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
      <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
        <div className="w-3 h-3 rounded-full bg-red-500"/>
        <div className="w-3 h-3 rounded-full bg-yellow-500"/>
        <div className="w-3 h-3 rounded-full bg-green-500"/>
        <span className="text-sm text-gray-600 ml-2">Demo Window</span>
      </div>
      <div className="relative h-[calc(100%-2rem)] bg-white">
        <Sidebar
          links={[
            { label: 'Home', href: '#', icon: Home },
            { label: 'Profile', href: '#', icon: User },
            { label: 'Settings', href: '#', icon: Settings },
            { label: 'Help', href: '#', icon: HelpCircle },
          ]}
          brandName="Demo App"
        />
      </div>
    </div>
  </TabItem>
  <TabItem value="code" label="Code">
    ```tsx
    import { Sidebar } from '@mindfiredigital/ignix-ui';
    import { Home, User, Settings, HelpCircle } from 'lucide-react';

    function SidebarDemo() {
      const links = [
        { label: 'Home', href: '#', icon: Home },
        { label: 'Profile', href: '#', icon: User },
        { label: 'Settings', href: '#', icon: Settings },
        { label: 'Help', href: '#', icon: HelpCircle },
      ];

      return (
        <Sidebar
          links={links}
          brandName="Demo App"
        />
      );
    }
    ```
  </TabItem>
</Tabs>

## Installation

<Tabs>
  <TabItem value="npm" label="npm" default>
    ```bash
    npx @mindfiredigital/ignix-ui add sidebar
    ```
  </TabItem>
  <TabItem value="yarn" label="Yarn">
    ```bash
    yarn @mindfiredigital/ignix-ui add sidebar
    ```
  </TabItem>
  <TabItem value="pnpm" label="pnpm">
    ```bash
    pnpm @mindfiredigital/ignix-ui add sidebar
    ```
  </TabItem>
</Tabs>

## Usage

Import the component:

```tsx
import { Sidebar } from '@mindfiredigital/ignix-ui';
```

### Basic Usage

```tsx
import { Home, Settings, User, Mail } from 'lucide-react';

function BasicSidebar() {
  const links = [
    { label: 'Home', href: '/', icon: Home },
    { label: 'Profile', href: '/profile', icon: User },
    { label: 'Settings', href: '/settings', icon: Settings },
    { label: 'Contact', href: '/contact', icon: Mail },
  ];

  return (
    <Sidebar
      links={links}
      brandName="My App"
    />
  );
}
```

## Variants

The Sidebar component comes with several variants to suit different use cases.

### Style Variants

<Tabs>
  <TabItem value="style-preview" label="Preview" default>
    <div className="space-y-4">
      <div className="w-full h-[300px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
        <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
          <div className="w-3 h-3 rounded-full bg-red-500"/>
          <div className="w-3 h-3 rounded-full bg-yellow-500"/>
          <div className="w-3 h-3 rounded-full bg-green-500"/>
          <span className="text-sm text-gray-600 ml-2">Default Theme</span>
        </div>
        <div className="relative h-[calc(100%-2rem)] bg-white">
          <Sidebar
            variant="default"
            links={[
              { label: 'Home', href: '#', icon: Home },
              { label: 'Settings', href: '#', icon: Settings },
            ]}
            brandName="Default Theme"
          />
        </div>
      </div>
      <div className="w-full h-[300px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
        <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
          <div className="w-3 h-3 rounded-full bg-red-500"/>
          <div className="w-3 h-3 rounded-full bg-yellow-500"/>
          <div className="w-3 h-3 rounded-full bg-green-500"/>
          <span className="text-sm text-gray-600 ml-2">Dark Theme</span>
        </div>
        <div className="relative h-[calc(100%-2rem)] bg-gray-800 flex">
          <Sidebar
            variant="dark"
            links={[
              { label: 'Home', href: '#', icon: Home },
              { label: 'Settings', href: '#', icon: Settings },
            ]}
            brandName="Dark Theme"
          />
          <div className="flex-1 p-4">
            <h3 className="text-lg font-semibold text-white">Dark Theme</h3>
            <p className="text-gray-300">Content area with dark theme</p>
          </div>
        </div>
      </div>
      <div className="w-full h-[300px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
        <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
          <div className="w-3 h-3 rounded-full bg-red-500"/>
          <div className="w-3 h-3 rounded-full bg-yellow-500"/>
          <div className="w-3 h-3 rounded-full bg-green-500"/>
          <span className="text-sm text-gray-600 ml-2">Light Theme</span>
        </div>
        <div className="relative h-[calc(100%-2rem)] bg-white flex">
          <Sidebar
            variant="light"
            links={[
              { label: 'Home', href: '#', icon: Home },
              { label: 'Settings', href: '#', icon: Settings },
            ]}
            brandName="Light Theme"
          />
          <div className="flex-1 p-4">
            <h3 className="text-lg font-semibold">Light Theme</h3>
            <p className="text-gray-600">Content area with light theme</p>
          </div>
        </div>
      </div>
      <div className="w-full h-[300px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
        <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
          <div className="w-3 h-3 rounded-full bg-red-500"/>
          <div className="w-3 h-3 rounded-full bg-yellow-500"/>
          <div className="w-3 h-3 rounded-full bg-green-500"/>
          <span className="text-sm text-gray-600 ml-2">Glass Theme</span>
        </div>
        <div className="relative h-[calc(100%-2rem)] bg-gradient-to-br from-blue-500 to-purple-600 flex">
          <Sidebar
            variant="glass"
            links={[
              { label: 'Home', href: '#', icon: Home },
              { label: 'Settings', href: '#', icon: Settings },
            ]}
            brandName="Glass Theme"
          />
          <div className="flex-1 p-4">
            <h3 className="text-lg font-semibold text-white">Glass Theme</h3>
            <p className="text-white/80">Content area with glass theme</p>
          </div>
        </div>
      </div>
    </div>
  </TabItem>
  <TabItem value="style-code" label="Code">
    ```tsx
    <Sidebar
      variant="default"
      links={links}
      brandName="Default Theme"
    />

    <Sidebar
      variant="dark"
      links={links}
      brandName="Dark Theme"
    />

    <Sidebar
      variant="light"
      links={links}
      brandName="Light Theme"
    />

    <Sidebar
      variant="glass"
      links={links}
      brandName="Glass Theme"
    />
    ```
  </TabItem>
</Tabs>

### Position Variants

<Tabs>
  <TabItem value="position-preview" label="Preview" default>
    <div className="space-y-4">
      <div className="w-full h-[300px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
        <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
          <div className="w-3 h-3 rounded-full bg-red-500"/>
          <div className="w-3 h-3 rounded-full bg-yellow-500"/>
          <div className="w-3 h-3 rounded-full bg-green-500"/>
          <span className="text-sm text-gray-600 ml-2">Left Sidebar</span>
        </div>
        <div className="relative h-[calc(100%-2rem)] bg-white">
          <Sidebar
            position="left"
            links={[
              { label: 'Home', href: '#', icon: Home },
              { label: 'Settings', href: '#', icon: Settings },
            ]}
            brandName="Left Sidebar"
          />
        </div>
      </div>
      <div className="w-full h-[300px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
        <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
          <div className="w-3 h-3 rounded-full bg-red-500"/>
          <div className="w-3 h-3 rounded-full bg-yellow-500"/>
          <div className="w-3 h-3 rounded-full bg-green-500"/>
          <span className="text-sm text-gray-600 ml-2">Right Sidebar</span>
        </div>
        <div className="relative h-[calc(100%-2rem)] bg-white">
          <Sidebar
            position="right"
            links={[
              { label: 'Home', href: '#', icon: Home },
              { label: 'Settings', href: '#', icon: Settings },
            ]}
            brandName="Right Sidebar"
          />
        </div>
      </div>
    </div>
  </TabItem>
  <TabItem value="position-code" label="Code">
    ```tsx
    <Sidebar
      position="left"
      links={links}
      brandName="Left Sidebar"
    />

    <Sidebar
      position="right"
      links={links}
      brandName="Right Sidebar"
    />
    ```
  </TabItem>
</Tabs>

### Direction Variants

<Tabs>
  <TabItem value="direction-preview" label="Preview" default>
    <div className="space-y-4">
      <div className="w-full h-[300px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
        <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
          <div className="w-3 h-3 rounded-full bg-red-500"/>
          <div className="w-3 h-3 rounded-full bg-yellow-500"/>
          <div className="w-3 h-3 rounded-full bg-green-500"/>
          <span className="text-sm text-gray-600 ml-2">Vertical Layout</span>
        </div>
        <div className="relative h-[calc(100%-2rem)] bg-white">
          <Sidebar
            direction="vertical"
            links={[
              { label: 'Home', href: '#', icon: Home },
              { label: 'Settings', href: '#', icon: Settings },
            ]}
            brandName="Vertical"
          />
        </div>
      </div>
      <div className="w-full h-[100px] relative border rounded-lg overflow-hidden bg-slate-100 shadow-lg">
        <div className="w-full h-8 bg-gray-200 flex items-center px-2 gap-2">
          <div className="w-3 h-3 rounded-full bg-red-500"/>
          <div className="w-3 h-3 rounded-full bg-yellow-500"/>
          <div className="w-3 h-3 rounded-full bg-green-500"/>
          <span className="text-sm text-gray-600 ml-2">Horizontal Layout</span>
        </div>
        <div className="relative h-[calc(100%-2rem)] bg-white">
          <Sidebar
            direction="horizontal"
            links={[
              { label: 'Home', href: '#', icon: Home },
              { label: 'Settings', href: '#', icon: Settings },
            ]}
            brandName="Horizontal"
          />
        </div>
      </div>
    </div>
  </TabItem>
  <TabItem value="direction-code" label="Code">
    ```tsx
    <Sidebar
      direction="vertical"
      links={links}
      brandName="Vertical"
    />

    <Sidebar
      direction="horizontal"
      links={links}
      brandName="Horizontal"
    />
    ```
  </TabItem>
</Tabs>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `links` | `Array<{ label: string; href: string; icon: React.ElementType }>` | `[]` | Array of navigation links with their icons |
| `brandName` | `string` | `'Brand'` | Name displayed at the top of the sidebar |
| `position` | `'left' \| 'right' \| 'bottomLeft' \| 'bottomRight'` | `'left'` | Position of the sidebar |
| `isOpen` | `boolean` | `true` | Whether the sidebar is expanded or collapsed |
| `variant` | `'default' \| 'dark' \| 'light' \| 'glass'` | `'default'` | Visual style variant |
| `direction` | `'horizontal' \| 'vertical'` | `'vertical'` | Layout direction |
| `onClose` | `() => void` | - | Callback function when sidebar is closed |
| `className` | `string` | - | Additional CSS classes |

## Customization

### Custom Styling

You can customize the appearance using the className prop and Tailwind CSS:

```tsx
<Sidebar
  className="bg-gradient-to-b from-purple-600 to-blue-600"
  links={links}
  brandName="Custom Sidebar"
/>
```

### With Custom Link Styling

```tsx
function CustomStyledSidebar() {
  const links = [
    { label: 'Home', href: '/', icon: Home },
    { label: 'Settings', href: '/settings', icon: Settings },
  ];

  return (
    <Sidebar
      links={links}
      brandName="Custom Links"
      className="[&_a]:hover:bg-blue-500 [&_a]:transition-colors"
    />
  );
}
```

### Responsive Sidebar

Create a responsive sidebar that adapts to different screen sizes:

```tsx
import { useState, useEffect } from 'react';

function ResponsiveSidebar() {
  const [isOpen, setIsOpen] = useState(true);

  useEffect(() => {
    const handleResize = () => {
      setIsOpen(window.innerWidth > 768);
    };

    window.addEventListener('resize', handleResize);
    handleResize();

    return () => window.removeEventListener('resize', handleResize);
  }, []);

  return (
    <Sidebar
      links={links}
      isOpen={isOpen}
      onClose={() => setIsOpen(false)}
      className="md:relative absolute"
    />
  );
}
```
